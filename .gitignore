# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Deploy artifact — built by `npm run build` into dist/, rsynced to the server app/.
# Never commit build output to source.
wordpress-plugin/autonomous-it-insights/app/

# Untracked deploy target (server coordinates) — see deploy.env.example
deploy.env
